import { SortingCyclePipe } from './sorting-cycle.pipe';

describe('SortingCyclePipe', () => {
  it('create an instance', () => {
    const pipe = new SortingCyclePipe();
    expect(pipe).toBeTruthy();
  });
});
